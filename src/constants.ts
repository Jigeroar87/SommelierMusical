import { User, Result } from './types';

// Legacy constants cleanup or replacement with new shapes if needed
// For now, removing the broken imports to satisfy linter while maintaining functional seeds in /data/
